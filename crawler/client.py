
# TODO: Raw HTTP request to take the response (HTML)
# TODO: parser to parse the html and extract the urls (absolute & relative path are the same just make them one url)
# TODO: URL normalization(the / at the end, the Capital letter, strip protocols), filtering, etc
# TODO: Main crawling logic
# TODO: do tests for all this modules
